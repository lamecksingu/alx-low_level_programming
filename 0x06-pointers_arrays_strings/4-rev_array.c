#include "main.h"

/**
 * reverse_array - a function to reverse array
 * of integers
 * @a: the array to be reversed
 * @n: Size of array
 */

void reverse_array(int *a, int n)
{
	int i = 1;

	while (n != 0)
	{
		a[n - 1];
		i++;
		n--;
	}
}
