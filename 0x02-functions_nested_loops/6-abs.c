#include "main.h"

/**
 * _abs - computes absolute value of a number
 * @n: The number to be checked
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
