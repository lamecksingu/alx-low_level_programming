#include "main.h"

/**
 * _isupper - checks for a digit between 0-9
 *
 * @c: interger to check digit
 *
 * Return: Always o (Success)
 */
int _isdigit(int c)
{
	for (c = 0; c <= 9; c++)
	{
		return (1);
	}
	else
		return (0);
	return (0);
}
