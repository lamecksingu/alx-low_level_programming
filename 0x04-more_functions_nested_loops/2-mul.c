#include "main.h"

/**
 * mul - a function to multiply two numbers
 * @a: first interger
 * @b: second interger
 *
 * Return: return multipled numbers
 */
int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
